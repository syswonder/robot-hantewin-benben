# Lifecycle interface required for rbnx boot to call Driver(CMD_INIT).
[contract]
id      = "robonix/primitive/arm/driver"
version = "1"
kind    = "primitive"
idl     = "lifecycle/srv/Driver.srv"
description = "Lifecycle control interface for arm primitive providers."

[mode]
type = "rpc"
